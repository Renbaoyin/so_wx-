<!--index.wxml-->
<view class='container'>
    <map id="map" longitude="{{longitude}}" latitude="{{latitude}}"                          scale="16" controls="{{controls}}" bindcontroltap="controltap" 
        markers="{{markers}}" bindmarkertap="markertap" 
        polyline="{{polyline}}" show-location>
    </map>

</view>
<view class="footer">
    <view bindtap='handlePublich'>发布</view>
    <view bindtap="handleSearch">搜索</view>
</view>
